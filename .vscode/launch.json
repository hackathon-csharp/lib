{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run datapack demo",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/datapack_demo",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build datapack demo",
            "stopAtEntry": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "externalConsole": false
        }
    ]
}
